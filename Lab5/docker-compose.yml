services:
  api:
    build: ./api
    ports:
      - "8004:8000"
    volumes:
      - ./db:/data

  ui:
    image: nginx:alpine
    ports:
      - "8084:80"
    volumes:
      - ./ui:/usr/share/nginx/html:ro
