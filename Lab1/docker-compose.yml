services:
  api:
    build: ./api
    ports:
      - "8000:8000"
    volumes:
      - ./db:/data

  ui:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./ui:/usr/share/nginx/html:ro
